info:
  name: list-wallets
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/wallets"
  headers:
    - name: X-API-Key
      value: "{{apiKey}}"
      enabled: true
  auth: inherit

docs: |
  The account's COLLECTION and DISBURSEMENT wallets with balances.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
