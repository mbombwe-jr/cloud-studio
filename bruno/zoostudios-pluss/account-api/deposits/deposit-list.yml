info:
  name: deposit-list
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/wallets/deposits?page=1&limit=20"
  headers:
    - name: X-API-Key
      value: "{{apiKey}}"
      enabled: true
  auth: inherit

docs: |
  Deposit history (paginated, optional &status= filter).

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
