info:
  name: transfer-list
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/wallets/transfers?page=1&limit=20"
  headers:
    - name: X-API-Key
      value: "{{apiKey}}"
      enabled: true
  auth: inherit

docs: |
  Transfer history with fee amounts (paginated).

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
