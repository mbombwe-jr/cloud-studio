info:
  name: list-staff-users
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/admin/users"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"
      enabled: true
  auth: inherit

docs: |
  ADMIN only. Lists all staff users.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
