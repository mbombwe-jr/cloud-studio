info:
  name: list-settlement
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/admin/accounts/{{accountId}}/settlement"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"
      enabled: true
  auth: inherit

docs: |
  ADMIN + SERVICEMAN. The account's settlement accounts.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
